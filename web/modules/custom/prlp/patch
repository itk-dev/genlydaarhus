diff --git a/config/install/prlp.settings.yml b/config/install/prlp.settings.yml
index 744229f..6a34955 100644
--- a/config/install/prlp.settings.yml
+++ b/config/install/prlp.settings.yml
@@ -1,2 +1,2 @@
 password_required: 1
-login_destination: user/%uid/edit
+login_destination: /user/%user/edit
diff --git a/src/Controller/PrlpController.php b/src/Controller/PrlpController.php
index 19854a1..59fd9f5 100644
--- a/src/Controller/PrlpController.php
+++ b/src/Controller/PrlpController.php
@@ -7,6 +7,7 @@ use Drupal\Core\Form\FormState;
 use Drupal\user\Controller\UserController;
 use Drupal\user\Form\UserPasswordResetForm;
 use Symfony\Component\HttpFoundation\Request;
+use Symfony\Component\HttpFoundation\RedirectResponse;
 use Symfony\Component\HttpKernel\Exception\AccessDeniedHttpException;
 
 /**
@@ -81,10 +82,17 @@ class PrlpController extends UserController {
       // Let the user's password be changed without the current password check.
       $token = Crypt::randomBytesBase64(55);
       $_SESSION['pass_reset_' . $user->id()] = $token;
-      return $this->redirect(
-        'entity.user.edit_form',
-        ['user' => $user->id()]
-      );
+
+      $login_destination = $this->config('prlp.settings')->get('login_destination');
+      if (!$login_destination) {
+        $login_destination = '/user/%user/edit';
+      }
+      if (substr($login_destination, 0, 1) !== '/') {
+        $login_destination = '/' . $login_destination;
+      }
+      $login_destination = str_replace('%user', $user->id(), $login_destination);
+
+      return new RedirectResponse($login_destination);
     }
 
     drupal_set_message($this->t('You have tried to use a one-time login link that has either been used or is no longer valid. Please request a new one using the form below.'), 'error');
diff --git a/src/Form/AdminSettingsForm.php b/src/Form/AdminSettingsForm.php
index 0e2b91e..cd2e714 100644
--- a/src/Form/AdminSettingsForm.php
+++ b/src/Form/AdminSettingsForm.php
@@ -47,7 +47,7 @@ class AdminSettingsForm extends ConfigFormBase {
     $form['login_destination'] = array(
       '#type' => 'textfield',
       '#title' => $this->t('Login Destination'),
-      '#description' => $this->t('User will be taken to this path after they log in with the password reset link. Token %uid can be used in the path, and will be replaced with the uid of the current user. Use %front for site front-page.'),
+      '#description' => $this->t('User will be taken to this path after they log in with the password reset link. Token %user can be used in the path, and will be replaced with the uid of the current user.'),
       // '#maxlength' => 64,
       // '#size' => 64,
       '#default_value' => $config->get('login_destination'),
