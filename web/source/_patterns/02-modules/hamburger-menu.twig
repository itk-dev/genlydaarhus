<div class="hamburger-menu--toggle js-hamburger-menu-toggle">
  <span></span>
  <span></span>
  <span></span>
  <span></span>
</div>
<nav class="hamburger-menu">
  <ul class="hamburger-menu--links">
    <li class="hamburger-menu--group">
    {% for item in hamburgermenuItems %}
      <a href="{{ item.url }}" class="hamburger-menu--link{{ item.in_active_trail ? " is-active" }}">{{ item.title }}</a>
    {% endfor %}
    </li>
  </ul>
</nav>