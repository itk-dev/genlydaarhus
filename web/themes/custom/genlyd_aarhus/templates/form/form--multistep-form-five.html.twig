{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}

{% set addActivity = {
  headingWithIcon: {
    title: ("Hello" | trans) ~ " " ~ user.account.name,
    subtitle: "Let us create an activity" | trans
  },
  practicalInformation: {
    title: "Practical information" | trans
  }
} %}

{% include "@components/activity/create-step-five.twig" with { form: element, addActivity: addActivity, progressBar: element.data.progressBar } %}