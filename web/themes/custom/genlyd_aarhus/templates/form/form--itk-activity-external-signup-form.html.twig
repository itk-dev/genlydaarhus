{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}

{% set externalSignup = {
  headingWithIcon: {
    title: "Sign up external user to activity" | trans,
    subtitle: "Let's sign somebody up to an activity" | trans
  }
} %}

{% include "@components/external-signup.twig" with { form: element } %}
