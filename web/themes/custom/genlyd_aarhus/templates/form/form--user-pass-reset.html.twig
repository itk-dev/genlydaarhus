{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}

{% set resetPassword = {
  headingWithIcon: {
    title: "Set your password" | trans,
    subtitle: "Here you must set your password." | trans
  }
} %}

{% include "@components/reset-password.twig" with { page: children, attributes: attributes, resetPassword: resetPassword } %}