{#
/**
 * @file
 * Theme override for a 'form' element.
 *
 * Available variables
 * - attributes: A list of HTML attributes for the wrapper element.
 * - children: The child elements of the form.
 *
 * @see template_preprocess_form()
 */
#}

{% set addActivity = {
  headingWithIcon: {
    title: ("Hello " ~ user.account.name) | trans,
    subtitle: "Let us create an activity" | trans
  },
  categories: {
    title: "Categories" | trans
  }
} %}

{% include "@components/activity/create-step-three.twig" with { form: element, addActivity: addActivity, progressBar: element.data.progressBar } %}