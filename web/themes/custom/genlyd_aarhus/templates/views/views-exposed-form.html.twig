{#
/**
 * @file
 * Theme override of a views exposed form.
 *
 * Available variables:
 * - form: A render element representing the form.
 *
 * @see template_preprocess_views_exposed_form()
 */
#}
{% if q is not empty %}
  {#
    This ensures that, if clean URLs are off, the 'q' is added first,
    as a hidden form element, so that it shows up first in the POST URL.
  #}
  {{ q }}
{% endif %}

{% set headingWithIcon = {
  title: "Search activities" | trans,
  subtitle: "Here you can search available activities" | trans
} %}

{% include "@components/activities-top.twig" %}

{{ form|without('actions', 'title', 'field_categories_target_id', 'field_zipcode_value') }}
